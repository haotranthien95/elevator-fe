# Quick Task 260403-o5k Plan
1. Move Next.js route `app/admin/reports/` to `app/admin/work-orders/` to disambiguate the layout component `href`.
2. Move `/api/admin/reports` Next.js handlers to `/api/admin/work-orders`.
3. Move `app/admin/analytics/` array components back to `app/admin/reports/` for UI mapping.
4. Correctly find and replace routing definitions `admin/reports` routing across components.
5. Fix `sidebar-nav.tsx` paths for Work Orders and Reports.
