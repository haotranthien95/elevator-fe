# Quick Task 260403-o1e Plan
1. Move `navItems` array out of `layout.tsx` and place it in `<SidebarNav>` client component entirely.
2. In `app/admin/layout.tsx`, invoke `<SidebarNav isAuthenticated={isAuthenticated} currentRole={currentRole} />` rather than passing the entire component array to prevent Server to Client serialization errors.
3. Verify next app builds correctly.
